{
  "name": "zenwalker.ru",
  "private": true,
  "devDependencies": {
    "zenpad": "0.0.1",
    "gulp-concat": "^2.2.0",
    "gulp": "^3.6.1",
    "gulp-autoprefixer": "0.0.7",
    "marked": "^0.3.2",
    "gulp-minify-css": "^0.3.4"
  },
  "scripts": {
    "build-prod": "gulp default && zenpad build",
    "build-html": "zenpad build -e development",
    "build-assets": "gulp default"
  }
}
